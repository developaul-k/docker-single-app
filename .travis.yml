sudo:: required
language: generic
services:
  - docker
before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t paul/docker-react-app -f Dockerfile
script:
  - docker run -e CI=true paul/docker-react-app npm run test -- --coverage
after_success:
  - echo "Test Success"